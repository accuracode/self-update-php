name: 'Continuous Integration'
on:
  push:
    branches:
      - $default_branch
  pull_request:
    branches:
      - $default_branch
  workflow_dispatch:

jobs:
  test:
    uses: accuracode/workflow-continuous-integration/.github/workflows/psalm.yml@5b5afc6da3b03c6331ca3e772c88bd913e189bc4
